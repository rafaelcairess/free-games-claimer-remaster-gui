# Core browser automation
nodriver>=0.50.3

# Process cleanup (kill orphaned Chrome process trees)
psutil>=7.2.2

# Device fingerprints for AliExpress mobile check-in
browserforge>=1.2.4

# Database
sqlalchemy[asyncio]>=2.0.52
aiosqlite>=0.22.1

# Notifications
# Capped below 2.0: the author calls 1.13 the last 1.x and says 2.x will break the API,
apprise>=1.13.0,<2.0.0

# Retry logic
tenacity>=9.1.4

# Scheduler
APScheduler>=3.11.3
tzdata>=2026.3

# OTP / 2FA
pyotp>=2.10.0

# Configuration
python-dotenv>=1.2.3

# Logging / Console output
rich>=15.0.0

# HTTP client
httpx>=0.28.1

# Tests (development only, not needed to run the bot)
pytest>=9.1.1
